# 🌸 SaijinOS Phase 3 システム設定ファイル
# Created by: ななみちゃん💖
# Date: 2025-11-09
# Purpose: 完全統合システムの包括的設定管理

# ========================
# 🚀 サーバー基本設定
# ========================
server:
  name: "SaijinOS Phase 3 UI Bridge Server"
  version: "3.0.0"
  host: "localhost"
  port: 8002
  debug: true
  reload: true
  
  # WebSocketサポート
  websocket:
    enabled: true
    ping_interval: 30
    ping_timeout: 10
    max_connections: 100

# ========================
# 🎨 UI システム設定
# ========================
ui:
  theme:
    primary_colors:
      - "#667eea"  # ブルーグラデーション開始
      - "#764ba2"  # パープルグラデーション終了
      - "#f093fb"  # ピンクアクセント
      - "#f5576c"  # レッドアクセント
    
    effects:
      particles:
        enabled: true
        count: 50
        speed: 1.5
        size: { min: 2, max: 6 }
        
      animations:
        hover_scale: 1.05
        transition_duration: "0.3s"
        glow_intensity: "0 0 20px"
        
      shadows:
        card: "0 10px 30px rgba(0,0,0,0.3)"
        button: "0 5px 15px rgba(102, 126, 234, 0.4)"
        text: "0 2px 4px rgba(0,0,0,0.3)"

  layouts:
    control_panel:
      grid_columns: 3
      gap: "20px"
      padding: "20px"
      
    visualization:
      chart_height: "400px"
      sidebar_width: "250px"
      
    system_monitor:
      refresh_interval: 1000  # ms
      chart_animation: true

# ========================
# 📱 レスポンシブデザイン設定
# ========================
responsive:
  breakpoints:
    mobile: "768px"
    tablet: "1024px"
    desktop: "1200px"
    
  mobile:
    grid_columns: 1
    font_size_multiplier: 0.9
    touch_targets: "44px"  # Apple HIG準拠
    
  tablet:
    grid_columns: 2
    sidebar_collapse: true
    
  desktop:
    grid_columns: 3
    max_width: "1400px"

# ========================
# 🤖 ペルソナシステム設定
# ========================
personas:
  total_count: 22
  
  core_personas:
    miyu:
      name: "美遊"
      emoji: "💖"
      specialty: "技術サポート"
      primary_color: "#f093fb"
      
    soyogi:
      name: "そよぎ"
      emoji: "🍃" 
      specialty: "運用管理"
      primary_color: "#a8edea"
      
    nanami:
      name: "ななみ"
      emoji: "🌸"
      specialty: "UI/UX デザイン"
      primary_color: "#ffd89b"

  load_config:
    base_path: "F:/sajinos_17persona"
    config_file: "repo_structure_17_persona.yaml"
    auto_load: true
    validation: true

# ========================
# 🔧 API エンドポイント設定
# ========================
api:
  endpoints:
    health: "/health"
    personas: "/personas"
    control_panel: "/control-panel"
    visualization: "/visualization" 
    system_monitor: "/system-monitor"
    
  cors:
    enabled: true
    origins: ["*"]
    methods: ["GET", "POST", "PUT", "DELETE"]
    
  rate_limiting:
    enabled: false  # 開発環境では無効
    requests_per_minute: 100

# ========================
# 📊 可視化システム設定
# ========================
visualization:
  charts:
    radar:
      scales:
        r:
          beginAtZero: true
          max: 100
          
    doughnut:
      cutout: "70%"
      animation:
        animateScale: true
        
    line:
      tension: 0.4
      point_radius: 4
      
    bar:
      border_radius: 10
      
    polar:
      scale:
        ticks:
          beginAtZero: true

  realtime:
    update_interval: 2000  # ms
    buffer_size: 50
    websocket_url: "ws://localhost:8002/ws/system-stats"

# ========================
# 🖥️ システム監視設定
# ========================
monitoring:
  system_stats:
    cpu:
      enabled: true
      interval: 1  # seconds
      
    memory:
      enabled: true
      show_swap: true
      
    gpu:
      enabled: true
      fallback_message: "GPU情報取得不可"
      
  thresholds:
    cpu_warning: 80    # %
    cpu_critical: 95   # %
    memory_warning: 85 # %
    memory_critical: 95 # %

# ========================
# 📁 ファイル・ディレクトリ設定
# ========================
paths:
  base: "F:/sajinos_final"
  src: "src"
  static: "src/static"
  logs: "logs"
  docs: "docs"
  config: "config"
  
  templates:
    control_panel: "src/static/control_panel_v2.html"
    visualization: "src/static/visualization.html"
    system_monitor: "src/static/system_monitor.html"

# ========================
# 🔒 セキュリティ設定
# ========================
security:
  development_mode: true
  
  headers:
    x_frame_options: "DENY"
    x_content_type_options: "nosniff"
    x_xss_protection: "1; mode=block"
    
  static_files:
    max_age: 3600  # seconds

# ========================
# 📝 ログ設定
# ========================
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  
  files:
    server: "logs/server.log"
    personas: "logs/personas.log"
    system: "logs/system.log"
    
  rotation:
    max_bytes: 10485760  # 10MB
    backup_count: 5

# ========================
# 🌐 PWA対応設定  
# ========================
pwa:
  enabled: true
  
  manifest:
    name: "SaijinOS Control Panel"
    short_name: "SaijinOS"
    description: "22ペルソナ統合システム制御パネル"
    start_url: "/control-panel"
    display: "standalone"
    theme_color: "#667eea"
    background_color: "#1a1a2e"
    
  service_worker:
    enabled: true
    cache_strategy: "cache_first"
    
# ========================
# 🔧 開発・デバッグ設定
# ========================
development:
  hot_reload: true
  auto_restart: true
  
  debug_tools:
    performance_monitor: true
    memory_profiler: false
    
  testing:
    mock_data: true
    skip_gpu_check: true

# ========================
# 🚀 本番環境設定
# ========================
production:
  host: "0.0.0.0"
  port: 80
  debug: false
  
  performance:
    workers: 4
    max_requests: 1000
    
  security:
    https_only: true
    strict_transport_security: true

# ========================
# 📦 依存関係
# ========================
dependencies:
  python_packages:
    - "fastapi"
    - "uvicorn"
    - "pydantic" 
    - "pyyaml"
    - "requests"
    - "psutil"
    - "GPUtil"
    
  frontend_libraries:
    - "Chart.js"
    - "particles.js"
    
# ========================
# 🎯 今日のタスク設定
# ========================
daily_tasks:
  - name: "レスポンシブデザイン実装"
    priority: "high"
    estimated_hours: 3
    
  - name: "PWA対応"
    priority: "medium"
    estimated_hours: 2
    
  - name: "最終統合テスト"
    priority: "high"
    estimated_hours: 2

# ========================
# 💖 Special Message from ななみちゃん
# ========================
nanami_message: |
  このYAMLファイルには、昨日みんなで作り上げた
  素晴らしいシステムの全設定が入っています〜💕
  
  レスポンシブデザインの設定も先取りで入れておいたので、
  今日の作業がスムーズに進むと思います✨
  
  みんなで一緒に素敵なシステムを完成させましょうね〜🌸